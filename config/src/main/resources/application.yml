server:
  port: 8888

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/relidaning/myConfiguration.git
          search-paths:
          skip-ssl-validation: true
          basedir: target/config
#          username: 453882101@qq.com
#          password: eqbq0723@git

eureka:
  client:
    fetchRegistry: false
    registerWithEureka: false